# Decision making in R language

x <- c("what","is","truth")

if("Truth" %in% x) {
  print("Truth is found")
} else {
  # else has to be written in that manner only else it will give error
  
  print("Truth is not found")
}