#vectors are used in R language.
vec1<-c(2,3,4,3,5,6,7,8)
#all the value are stored in the vec1.
#another way to write the vector.
vec2<-c(1:8)
#to find the length of the vector
length(vec2)
#for the addition of the two vectors 
result=(vec1+vec2)
print(result)

